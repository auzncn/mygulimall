spring:
  datasource:
    username: root
    password: root
    url: jdbc:mysql://192.168.222.128:3306/gulimall_sms
    driver-class-name: com.mysql.jdbc.Driver
  jackson:
    date-format: yyyy-MM-dd HH:mm:ss
  application:
    name: coupon
  cloud:
    nacos:
      discovery:
        server-addr: 127.0.0.1
mybatis-plus:
  mapper-locations: classpath:/mapper/**/*.xml
  global-config:
    db-config:
      id-type: auto
      logic-delete-value: 1
      logic-not-delete-value: 0
server:
  port: 40004
logging:
  level:
    com.atguigu.gulimall: debug


